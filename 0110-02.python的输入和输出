目标：
掌握python的输入和输出

课程内容
a.输出的应用场景
b.输入的应用场景
c.python的输出
d.python的输入


a.输出的应用场景
a1.taobao上搜索lv后页面返回内容
a2.访问页面不存在(404)
a3.调试程序时候进行输出
a4.网站提示输入用户名、密码

b.输入的应用场景
b1.填写注册信息
b2.taobao上填写搜索内容
b3.前后端交互

c.python的输出
a.直接输出
b.变量输出
c.函数输出
d.格式化输出

d.python的输入
input      python2中接受键盘输入的数字下信息，python3中接受键盘输入的字符信息
raw_input  python2中接受键盘输入的字符信息，python3中已经废弃
补充:判断数据类型
type()
